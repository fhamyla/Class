# Class
The Attendance Management System is a web-based application with role-based access for administrators and teachers to manage student attendance. It features responsive and accessible design for use on any device and uses PostgreSQL as the database for secure and efficient data management.

## Getting Started

1. Run `npm install`
2. Run `npm run dev`
